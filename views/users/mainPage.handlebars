<section>
    <h1>Student information</h1>
    <dl>
        <dt>Last Name</dt>
        <dd>{{user.lastName}}</dd>
        <dt>First Name</dt>
        <dd>{{user.firstName}}</dd>
        <dt>Student ID</dt>
        <dd>{{user.studentId}}</dd>
        <dt>Year</dt>
        <dd>{{user.year}}</dd>
        <dt>Credits</dt>
        <dd>{{user.credits}}</dd>
        <dt>Finished Courses</dt>
        <dd>{{user.finishedCourses}}</dd>
        <dt>Current Courses</dt>
        <dd>{{user.currentCourses}}</dd>
        <dt>Registered Courses</dt>
        <dd>{{user.registeredCourses}}</dd>
    </dl>
</section>
<section>
    <h1>Main Page</h1>
    <ul>
        <li><a href = "/courses/registration/">Registration</a></li>
        <li><a href = "/courses/rating">Rating</a></li>
    </ul>
</section>

<div class="logout-btn">

        <button class="cust-button" type="button" onclick="window.location.href='./logout'"> Logout </button>
        
</div>